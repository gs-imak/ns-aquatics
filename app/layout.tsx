import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Swimming Coach Malaysia | Expert Kids & Adult Training",
  description: "Professional swimming coaching in Malaysia. Specializing in kids training, stroke correction, and personalized programs. Transform your child's swimming skills today!",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
